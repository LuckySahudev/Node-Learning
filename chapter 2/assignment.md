1. Capture the request which goes when you like a post on Facebook (using Chrome Network). What are the headers? What is the payload?
Ans. 




2. Modify the server (which currently uses only URL switch) to also handle both METHOD (GET, POST) and URL path. Ensure that the output of GET /demo is different from POST /demo.
Ans.    
    in part 2 we use the postman post request by same link in post mode . 

3. Run 2 different servers using the same code (index.js) on 2 different ports. Can you do it using the same file and change ports dynamically?






4. Send data to the server using /demo?product=123 (query parameters). Can you capture that data and make the product page work according to the given ID (123)?