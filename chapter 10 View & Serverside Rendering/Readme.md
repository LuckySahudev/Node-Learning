1.  View -> it is part of mvc
    
    -> in view we only create a folder  view/pages
    -> in that folder we store html templates which are use in frontend
    -> not many things in it 

2.  Server side Rendering 

    -> in server side rendering we use to create html template according to query
    